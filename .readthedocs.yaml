# Read the Docs 构建配置（v2）
# 导入仓库后在 readthedocs.org 启用，每个 git tag 自动构建一个版本站点

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  apt_packages:
    - doxygen

sphinx:
  configuration: source/conf.py
  fail_on_warning: true

python:
  install:
    - requirements: requirements.txt
